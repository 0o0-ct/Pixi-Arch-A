#!/usr/bin/env bash
# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Launcher for kitty. The terminal theme is static (dark semi-transparent
# with blur, defined in kitty.conf); only the fastfetch Arch logo rotates
# via random-logo-colors.py. Kept as a launcher so keybinds stay intact.

exec kitty "$@"